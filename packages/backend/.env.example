# Anthropic API key used by the agent loop. Not required for the standalone
# POST /api/compile smoke test.
ANTHROPIC_API_KEY=

# Port the Fastify server listens on.
PORT=3001

# Model used for the agent loop.
CLAUDE_MODEL=claude-sonnet-5
